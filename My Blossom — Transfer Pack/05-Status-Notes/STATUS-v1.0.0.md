# STATUS — session handoff ledger

> The fast resume point. Read this + `CLAUDE.md` + `docs/MERGE-SPEC.md` to know where we are without re-scanning the tree. Newest first.

**Last updated:** 2026-06-23 · **Version:** v1.0.0 (W0 done · W1+W2 partial · **shipped APK · pushed to GitHub · growth loop LIVE**) · **Stack:** RN (Expo SDK 54) + Supabase, Android-first

---

## ⚖ Honest progress (read first)
The **merged spine** is in and now carries a real **Productivity vertical**: 7 widgets, the gamification core, and a working shell (module switch · page tabs · FAB add-gallery). Still mostly engine + first-module by felt-product measure (**~12–18%**), but the pattern is now proven across 7 diverse widgets, so each remaining Blossom widget is a clone-the-template job. Track felt-product % separately from engine %.

## Now (in progress)
- **Everything ships OTA now.** Simon installed the `expo-updates` APK (`3a6ec2ac`); since then, growth loop + flower + toast + settings panel + Activity module + "add a module" all go out via `eas update --branch preview`. On app launch it pulls the latest preview bundle. To get the Activity module on an existing install: tap the module name ▸ **ADD A MODULE ▸ Activity**.
- **W1 remaining:** 3-window module rail (active-centred) + drag-reorder + soft-delete.
- **W2 remaining:** the link/outputs system (`ctx.readLink`) → then the container Tools that need it: **Health** (vine of nested/linked quests) · **routine** · **Market** (needs a coin-earning hook first). 
- **Then:** W3 Study garden. **Later publish:** `production` AAB + `eas submit` → Play Store.

## Done this session (newest first)
- **W1: "add a module from preset" in the switcher.** The module-switcher sheet now lists the modules you have, then an **ADD A MODULE** section of presets (My Blossom · Productivity · Activity); tapping one `instantiateModule`s it into the store and switches to it. So existing installs gain **Activity** (and its Physical flower) over-the-air — no reinstall. Verified: tsc clean · smoke 18/18 · android export green.
- **W2: the Activity module — a second (Physical) flower, no new Tool code.** Two clean enablers: (1) preset `config` now **seeds a Tool's state** (`engine.buildWidget` merges `config` over `defaults()`), so a preset can point a flower at an aspect or retarget a Tool's growth; (2) the four completion Tools gained a configurable **`growthAttribute`** (default = their natural one), so the same habit grows Discipline in Productivity but **Strength** in Activity. New `presets/activity.ts` (feeds `physical`, theme `ocean`, Today + Body pages with a `physical` flower) — seeded on fresh installs + in the preset gallery. Verified: smoke 18/18 (+retarget check) · tsc clean · android export green.
- **W1: generic settings panel (data-driven from the `settings` schema).** New **`SettingsSheet`** renders a Tool's declarative `settings` as a real form (select chips · text · number · boolean) and writes the Tool's own state object via the Store — so the open Tool updates live (WidgetHost is subscribed), zero per-Tool code. A ⚙ button appears in the full-view bar for any Tool that declares settings. First consumer: the **flower's `aspect` select** (pick which flower a card shows). Verified: tsc clean · smoke 17/17 · android export green.
- **OTA pipeline live + cozy growth feedback.** Added `expo-updates` + `eas update:configure` (updates.url, runtimeVersion `appVersion`, channels in eas.json) → published the **first OTA update** to branch `preview` (group `f2b08c75`). Built a soft **`ToastProvider`** (animated, calm, one-at-a-time) and wired `ctx.toast`; `WidgetHost` now confirms every completion with "🌱 +10 Discipline" as it feeds the flower. Gitignored `.claude/settings.local.json` (was tracked). Verified: tsc clean · smoke 17/17 · android export green.
- **The Blossom growth loop is LIVE — activity now grows flowers.** Wired the whole vertical: the Tool contract gained a pure `logic.grows(prev,next,action)` (says what *attribute*+amount a change earned — diffed so re-taps never double-award) and `ctx.growth` (read side). `WidgetHost` forwards contributions to `ctx.grow` with the module's `feedsAspect`. New **`GrowthProvider`** owns the single `growth` ledger (loads/persists/syncs like any object). Added `grows` to **habit→discipline · quest→focus · goal→wisdom · skill→learning**. Built the **`AspectFlower`** (pure-RN radial petals, one per attribute, scaled by level — no SVG/Skia) and a new **`flower` Tool** (8 Tools now) that visualises any aspect (card = flower; full = aspect switcher + petal breakdown), seeded on Productivity ▸ Growth. **Verified: smoke 17/17** (incl. 5 new growth-loop checks) **· tsc clean · android export green (4.4 MB).** Decoupled per the merge rules: Tools name attributes, the module names the aspect, the flower only reads.
- **GitHub connected + pushed ✅.** Ran the full CONNECT.md sequence (cleared a stray nested `.git` + stale lock → remote → commits → `git push --force`); `Simonatofficial/My-Blossom` `main` is now v1.0.0 (Simon added the `Bash(git push:*)` allow-rule). **First standalone `preview` APK shipped + booting on Simon's phone** (hub renders, FAB works).
- **Bootstrapped, re-verified the mer