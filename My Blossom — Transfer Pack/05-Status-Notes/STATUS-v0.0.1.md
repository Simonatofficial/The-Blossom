# STATUS — session handoff ledger

> The fast resume point. Read this + `CLAUDE.md` to know where we are without re-scanning the tree.
> Keep it current per `docs/00-operating-framework.md` §4. Newest first.

**Last updated:** 2026-06-23 · **Version:** v0.0.1 (build 2) · **Stack:** React Native (Expo) + Supabase, Android-first

---

## ⚖ Honest progress (READ FIRST — the phases are NOT equal)

> The M0–M7 list makes "5 of 8" look ~60% done. It isn't. The phases are wildly unequal:
> - **M0–M4 = the engine** (storage, sync, theme tokens, FX plumbing). Real, correct, ~done — but **invisible**. This is the boring, hard-to-rebuild ~30% of the *work* and ~0% of what you *see and feel*.
> - **M5 = the actual app** — the six modules, the pages, the real tools, the Liri, the canonical cozy look and content. **This is ~80% of the product you'll use and love, and it has barely begun (slice 1).**
> - **Truer gauge:** of "the app you'll actually feel," we're roughly **~10–15%** in — standing on a solid foundation, not finished.
> Never let a phase checkmark imply the cozy app exists. It doesn't yet; M5 is where it gets built. Track **felt-product %** separately from **engine %**.

> **🟢 VISUAL ENGINE — PORTED & approved; ready to wire in (Claude Code's next job).** Simon approved the prototype and said "port it over." Full cross-platform Canvas2D engine done, **driven by the app's real themes** (`src/presets/themes.ts`): `src/fx/engine/` = `scenes.ts` + `scene.ts` + `engineRuntime.ts` (parse-verified) + `engineHtml.ts` + `VisualEngine.tsx`/`.web.tsx`. Covers all 7 themes, atmospheres (dayNight/constellations/sunset/sunrise/waves/solarSystem), the full particle roster, + optional whole-bloom flower. **→ Claude Code: follow `docs/ENGINE-PORT.md`** — `npx expo install react-native-webview`, swap `<Sky/>`+`<AtmosphereCanvas/>` for `<VisualEngine themeId={t.id}/>` on every screen, delete `engine/themes.ts`, retire Skia, verify web+phone. Then resume M5 (modules/tools) on top.

> **(superseded) 🟡 VISUAL ENGINE — prototype shipped, awaiting Simon's verdict.** Root cause of the bad visuals: M3 used **Skia** → blobs + skipped on web. **Simon chose "prototype the port first."** Built a **cross-platform Canvas2D engine** (same as the old app + mockups): `src/fx/engine/engineHtml.ts` (self-contained HTML/Canvas2D: themed sky + celestial + hills + particles + whole-bloom flower), `themes.ts` (Flower/Sunset/Cosmos), and `VisualEngine` with platform wrappers — **`.web.tsx` = `<iframe srcDoc>`**, **native `.tsx` = transparent `<WebView>`** — so **web renders identically to phone**. Demo at **`/engine`** (`app/engine.tsx`): native cards float over the living sky + a theme switch. **To run native: `npx expo install react-native-webview`** (Expo-supported); web needs nothing. **If Simon likes it → commit:** swap Skia FX for `VisualEngine` on the real screens, port the full old-app theme/atmosphere/particle roster, retire Skia. See `docs/BUILD-NOTES.md`.

## Now (in progress)

- **M5 — Build out the real modules & aspects (Productivity & Activity first).** First slice shipped; growth engine + modules next.
  - **✅ Slice 1 — the Blossom aspect view (shipped + headless-verified).** Per Simon's feedback the flower now lives **inside the aspects**, not as a standalone home centerpiece. New: `src/presets/aspects.ts` (the 5 aspects as DATA — attributes from DESIGN-DOC Floor 1.3, placeholder levels), `src/fx/AspectFlower.tsx` (a flower per aspect, **pure RN views** so it renders on phone **and** web — petals = attributes with their level number, glowing seed), `app/blossom.tsx` (the "Your Blossom" page: all 5 aspect cards `Name · Lv X`). Home retired the standalone `<Flower/>` → a calm "Your Blossom" link. Verified: tsc green, `scripts/smoke-aspects.ts` pass, android export clean, web `/blossom` 200. **Foundation, not final look** (numbers-on-petals are basic; gentle motion + colour-by-level + skill stars are polish).
  - **⏭ Next M5 steps:** (1) **growth engine** — modules emit aspect-XP events (attribute/skill tagged) that raise attribute → aspect levels (docs/06 §3), replacing the placeholder `level:1`s so petals actually grow; (2) **skill stars** orbiting each flower (docs/06 §1); (3) **port the Productivity module** (Mental) then **Activity** (Physical) from the old Blossom's logic — real tools, the Blossom loop wired in; (4) the **Liri page** whole-bloom + radar (the existing `src/fx/Flower.tsx` is kept for this).
  - **🎨 Standing feedback (Simon, 2026-06-23):** theming + atmospheres are a **foundation to expand heavily, old-app ("The Blossom")-inspired**, with **much more customizability** — NOT the final look. He may revise the framework/docs for this. Flower-in-aspects = done per his reference screenshot. See [[settings-and-foundations]].

## Next (queued, in order)

1. **Finish M5** — growth engine + skill stars + Productivity & Activity modules (Blossom loop), then the Liri page. (`docs/06`, `docs/05`, DESIGN-DOC Floor 1/5/6.)
2. **Theming/customizability overhaul** — expand themes/atmospheres + a custom theme creator, inspired by the old Blossom (Simon's standing ask; pairs with the Daisy tier in `docs/02`).
3. **M6 — native extras** (home widgets, push, background alarms), **M7 — money + release** (RevenueCat/Stripe tiers, store submission). (`docs/02` §D–H, `monetize`, `release-it`.)
4. **MVP cut line:** Blossom module (Home/Blossom) + core widgets + sync + tier gate → ship focused (`docs/03`).
5. **Deferred from M3b (need device-tested gestures / install):** tappable interactive effects (must NOT block ScrollView — gesture Tap that only claims a touch hitting a particle via `particleAt`) + the standalone preview build (airplane-mode offline proof, build c3c2d120).
6. **Sync polish (non-blocking):** set a real Supabase Site URL so the email-confirm page isn't the default broken `localhost:3000`; add a uuid/Postgres-strict fake to `smoke-sync.ts` so it catches id/type mismatches like the live `400`.

## Done (recent, newest first)

- **v0.0.1 (build 2) — M5 slice 1: the Blossom aspect view.** Flower moved inside the aspects (Simon's feedback): 5 aspects as data (`src/presets/aspects.ts`), a cross-platform `AspectFlower` (pure RN views — petals = attributes + level number + glowing seed), a `Your Blossom` page (`app/blossom.tsx`), home now links to it instead of the standalone bloom. tsc green, `smoke-aspects.ts` pass, android export clean, web `/blossom` 200. Foundation to refine (motion, colour-by-level, skill stars) alongside the theming overhaul.
- **v0.0.1 (build 2) — M4 cloud sync DONE + device-verified end-to-end.** Supabase sync behind the M1 `store` interface (no widget changed): additive `SyncSource` (LWW `applyRemote` + `changedSince`) on sqlite+memory, transport-agnostic `SyncEngine` (push-then-pull, cursors), `RemoteStore` + real `SupabaseRemote` + `FakeRemote`, null-safe lazy Supabase client (offline when unconfigured; session via sqlite Prefs), anonymous→email auth, `SyncProvider` (foreground/background/interval/Realtime), Account card in Settings, hardened `supabase/schema.sql`. Activated live (project `ayxeoaxnvypjiqrlumiz`, keys via `.env`+`app.config.js`, RLS + 4 policies verified via Supabase MCP). Fixed a live `400` (objects.id `uuid`→`text` for friendly ids) and made `WidgetHost` subscribe so pulled changes repaint. **Phone synced 4 real objects to the cloud; web (`output:single`, in-memory store, Skia skipped) works as the 2nd device.** Headless: `smoke-sync.ts` (two-device LWW + soft-delete), tsc, android export all green.
- **v0.0.1 — M3b FX shipped: living atmospheres + colourful Flower-graph.** Stars brought to life (were dead pixels — now breathe + drift); soft glow on every particle (BlurMask); the **whole-bloom Flower-graph** is the home centerpiece (`Flower.tsx`, pure `flowerLayout.ts` geometry, 4 colourful aspect petals + 8 faint back petals + glowing seed + gentle float, in an `ErrorBoundary`). All device-verified on the dev build. Deferred to next session (need device-tested gestures): tappable interactive effects (must not block scroll) + a final standalone preview build for the airplane-mode offline proof.
- **v0.0.1 — DEV BUILD live; atmospheres + Settings tab on device.** Stood up the EAS cloud development build (Node exec-policy + `eas login` hurdles cleared; `eas init` → `@cozycosmo/my-blossom`; APK installed — a real standalone app). Skia **AtmosphereCanvas** renders theme-signature particles drifting behind the cards; **Liveliness** dial (still/gentle/lively, reduced-motion aware) persists; moved the Atmosphere + Liveliness controls into a new **Settings tab** (gear on home) backed by `LivelinessProvider`. Particles are a deliberate first pass (plain circles) — crafted shapes/glow pending (docs/04, a breathe-life polish).
- **v0.0.1 — M3a theming SHIPPED.** Data-driven theme system: 7-theme roster (Cosmos·Flower·Forest·Ocean·Sunset·Autumn·Scarlet) as presets (`src/presets/themes.ts`); `ThemeProvider` restores + persists the choice via the M1 prefs store; gradient **Sky** backgrounds (`src/ui/Sky.tsx`, expo-linear-gradient — Expo-Go-safe) with a horizon haze for depth; translucent floating Tool cards (`withAlpha`); themed status bar; and a calm **Atmosphere** picker (`src/ui/ThemePicker.tsx`) on the home. Verified: typecheck green, `smoke-themes.ts` pass, android export 1401 modules. (Animated scenery + particles + the Flower-graph = M3b on the dev build.)
- **v0.0.1 — M2 first Tools CONFIRMED ON DEVICE.** Simon sees Notes, Tracker, Quest rendering. Plugin contract + registry + WidgetHost validated: each Tool is self-contained, pure `logic.ts` + `View.tsx`, persists through the M1 Store.
- **v0.0.1 — M1 storage CONFIRMED ON DEVICE.** Simon scanned with SDK 54 Expo Go; the home rendered and the `meta/home` round-trip showed "tended 1 time · planted 6/22/2026" — local sqlite read/write working on a real phone. The `Store` interface + sqlite/memory adapters + prefs are live. (Built in the M0-renders entry below.)
- **v0.0.1 — pinned to Expo SDK 54 (Expo Go cap on Simon's device).** Simon's Android can't get an Expo Go newer than **SDK 54** (the Play Store serves the Expo Go matching the device OS; it won't upgrade to 56). Reconciled the whole project 56 → 54 via `npm i expo@~54` + `expo install --fix` (React 19.1.0, RN 0.81.5, expo-router 6.0.24, expo-sqlite 16, Reanimated 4.1.1 + worklets 0.5.1 — babel worklets plugin still correct). Removed `expo-status-bar` from `app.json` plugins (not a config plugin; SDK 54 warns). Re-verified green (typecheck, smoke test, android export 1380 modules). **Decision:** stay on Expo Go through M1–M2; at **M3** move to a **development build** (needed anyway for Skia/mmkv/purchases/home-widgets — none run in Expo Go) and can bump SDK then. We keep the cozy Expo Go loop now without the build-tooling tax.
- **v0.0.1 — M0 RENDERS (toolchain live, deps reconciled).** Installed Node.js LTS (v24.17.0 / npm 11) via winget — it was absent, the one blocker to the whole build loop. Ran the dependency install and reconciled **every** version to **Expo SDK 56** via `expo install --fix` (expo-router was mis-pinned `~6.0.0`; now `~56.2.11`; RN 0.85.3, React 19.2.3, Reanimated 4.3.1, etc.). Fixes along the way: added **`.npmrc` (`legacy-peer-deps=true`)** — npm 11 enforces RN's over-strict peer ranges and Expo's tooling assumes a tolerant resolver; pointed `babel.config.js` at **`react-native-worklets/plugin`** (Reanimated 4 relocated it); added web-target deps (`react-dom`, `react-native-web`) + expo-router peers (`expo-linking`, `expo-constants`, `expo-system-ui`) — web is our **headless render check** (docs/02: "web for testing only"). **Verified:** `tsc --noEmit` green; `expo export --platform web` bundles 1218 modules clean and the static HTML renders the themed "My Blossom — a quiet garden" home → the M0 shell (Expo Router + ThemeProvider) works end-to-end (same code runs on Android via Expo Go). ⚠️ Not yet on GitHub — `git init` + first commit done locally; **push needs Simon to create the remote** (`SETUP.md` §3). **Next: M1 storage layer.**
- **v0.0.1 — DESIGN DOC FOLDED IN (decisions locked).** Simon completed the staircase; his answers are now the framework. Saved his spec as **`docs/DESIGN-DOC.md` (the product source of truth)**. Key locks: **5 aspects** (Mental·Physical·Emotional·Social·**Recreation**) via **Aspect→Attribute→Skill**, rendered as flowers (petals=attributes, stars=skills) + a Liri radar; **6 modules** (hub **My Blossom** + **Productivity·Activity·Meditation·Connection·Recreation**) — **build Productivity & Activity first**; companion **"Liri"** (own page + dock + subtle on pages, "Liri Life" duck-life game, bond/mood/journal/milestones, coins buy food/toys/clothes); element map Air/Water/Earth/Fire, 15-q quiz, sub-element evolves & locks, form swap caps (Cosmos to change); **Tools vs Widgets** (Android home-screen); **no-paywall** model + 7-day Cosmos trial + Settings-only subscriptions; **Scarlet theme** + custom theme creator at launch (Designer+); **atmospheres absorb weather** + interactive effects with minigame counters (fireflies/pufferfish/meteors); **Supabase hardening mandatory** (`docs/02`); soft-delete window now **1 week**. New **`docs/06`** (structure map) + **`organized-code` skill** (11 skills) for clean, overhaul-ready code + a `BUILD-NOTES.md`/`CODE-MAP.md` practice. Updated docs 01/02/04/05 + CLAUDE.md. **Next: begin building bottom-up (M0→M1→M2), Productivity & Activity prioritized.**
- **v0.0.1 — canonical look LOCKED.** Simon approved the cohesive home/companion direction. Added `docs/04` §8 (Canonical screen layout): **modules on a top 3-window rail (active centred, arrows/swipe L/R), pages on a bottom tab bar**, companion-avatar shortcut, FAB. **Flower-graph = a whole bloom** (even 8-petal back layer + 4 vivid level-scaled aspect petals), centred on Home. **Companion page drops the "element fixed" label.** Backgrounds use the original themed atmospheres (Flower/Sunset/Cosmos shown), softened toward painterly (ongoing refinement, away from cartoonish). Translucent crafted cards throughout. This is the design north star for the build.
- **v0.0.1 — the heart defined.** Added `docs/05-companion-and-elements.md` (draft): the soul-bonded **elemental creature** companion, the 16personalities-style **element quiz** (fixed element, swappable creature form), aspect-driven **creature growth** (Physical→size, Mental→abilities, Emotional→colors, Social→beauty), the **Blossom loop** (Study→Mental, Exercise→Physical, Breathing→Emotional, Connection→Social), the **self-contained Tools** rule (fixes the old cross-widget bugs), and **Pantheons + book-world** as future/exploratory. Reconciled `docs/04` (companion = elemental creature) and `docs/01` (independent Tools + aspect loop). Open questions flagged for `grill-me`.
- **v0.0.1 — visual soul defined.** Added `docs/04-art-direction.md` (the living-garden art direction: layer-cake depth, per-theme worlds, the Sprout companion, tactile materials, ambient motion, earned delight, anti-slop list) + the `breathe-life` skill (10 skills total) that enforces it and reconciles with `cozy-check` (rich world, quiet controls). This is the design north star for all visual work — every surface inherits it.
- **v0.0.1 — project planted.** Fresh React Native (Expo) + Supabase scaffold created in its own folder. Includes: `CLAUDE.md`, operating framework (`docs/00`), architecture + build order (`docs/01`), zero→release roadmap (`docs/02`, facts verified June 2026), market+strategy (`docs/03`), the research library (`docs/research/` — market research + technical teardowns), all 9 skills (`overhaul-the-ask`, `cozy-check`, `ship-it`, `usage-check`, `grill-me`, `cloud-sync`, `monetize`, `release-it`, `learn-from-the-field`), Expo config (`app.json`/`eas.json`/`package.json`/`tsconfig`/`babel`/`.gitignore`/`.env.example`), a runnable M0 shell (`app/_layout.tsx`, `app/index.tsx`, `ThemeProvider`), and the `Store` interface (`src/core/store/types.ts`). Earlier prototypes (sibling folders) are reference-only; salvage per `docs/01` §7.

---

### How to use this file
- **Starting work:** move the task into **Now**, add its one-line frame (Goal · Approach · Files · Done-when).
- **Finishing:** move it to **Done** with its `v0.x.y` tag; pull the next item into **Now**; bump *Last updated* + *Version*.
- **Stopping mid-task:** leave it in **Now** with a `⏸ next step:` note.
