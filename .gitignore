.DS_Store
Thumbs.db
desktop.ini

# Optional cloud-sync secrets (V2 §1) — never commit real keys
config.js
.env

# Local Claude Code tooling (machine-specific paths; not for the public repo)
.claude/*
# ...but DO version the project skills so they sync across sessions/machines
!.claude/skills/
# grill-me is third-party, fetched via curl (see CLAUDE.md) — keep it local
.claude/skills/grill-me/
